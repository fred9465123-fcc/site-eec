            parent.appendChild (
                function () {
                    function normalizeText(text) {
                        return text === undefined || text === null ? '' : String(text);
                    }

                    function createElement
                }
            )